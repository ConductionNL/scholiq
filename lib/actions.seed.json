{
  "$comment": "ADR-023 action matrix seed for scholiq. Default admin-only; broaden via Admin Settings > Scholiq > Action authorization.",
  "actions": {
    "audit-pack.export": ["admin"],
    "qti.import": ["admin"],
    "rollover.plan": ["admin"]
  }
}
